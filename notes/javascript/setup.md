Setting up the environment with the package.json - npm init

In the package.json file add these two sections:

  "dependencies": {
  },
  "devDependencies": {
  }

In here the third party librariers will be put in